# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/

# macOS
.DS_Store

# Editors
.idea/
.vscode/
*.swp
*.swo

# Leerie run artifacts — only present when leerie is run directly in this
# repo (dogfooding). Target repos no longer accumulate .leerie/ directories;
# state lives in $HOME/.leerie/<basename>/ by default. Ignore the run
# state/logs, but DO track the committable per-repo config + Dockerfile
# (leerie:813 "commit this file to version-control").
.leerie/*
!.leerie/config.toml
!.leerie/Dockerfile
# Top-level log when the CLI is teed (`./leerie "..." | tee leerie.log`).
# The `leerie-*.log` form covers per-task tees, e.g.
# `leerie prompts/fix-x.md | tee leerie-fix-x.log`.
leerie.log
leerie-*.log

# Local Claude Code settings / cache — per-machine, never tracked.
.claude/

# Working notes — kept locally as a reference, not a deliverable.
foo.txt

# Environment files — never tracked. May hold secrets (API keys,
# tokens, DB credentials). The `.env.example` template form is
# allowed through explicitly so projects can commit a documented stub.
.env
.env.*
!.env.example

# Test artifacts
.pytest_cache/
.coverage
htmlcov/
